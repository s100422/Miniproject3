# 배당 모아 해외여행

## 0. 기본 정보
- 이름 : 문수진
- 배포 주소 : https://dividend-travel-planner.vercel.app/
- 사용한 AI 모델/API : Gemini 2.5 Flash (Google Generative Language API, REST 직접 호출) + 참고용으로 Yahoo Finance 시세 API(현재가 스냅샷, AI 아님)

## 1. 한 줄 소개
매달 여행비만큼의 배당금을 받고 싶은 초보 배당투자자에게, 배당킹·배당귀족 종목 중에서 1월부터 12월까지 빠짐없이 배당이 들어오는 조합을 AI가 짜주는 배당 포트폴리오 플래너.

## 2. 이런 사람을 위해 만들었습니다
- 대상 : 목돈보다는 "매달 들어오는 여행비" 정도의 현금흐름을 목표로 배당투자를 막 시작하려는 초보 투자자
- 그 사람이 지금 겪는 불편 : 어떤 배당주를 얼마씩 사야 매달 끊기지 않고 배당이 들어오는지, 그 조합으로 목표 금액까지 몇 년이 걸리는지를 스스로 계산하기 어려움
- 이 서비스를 쓰면 달라지는 것 : 목표 월배당금액과 매달 투자 가능한 금액만 입력하면, AI가 짜준 포트폴리오 후보 2개와 30년치 배당성장 시나리오, 월별 배당지급 캘린더를 바로 받아볼 수 있음

## 3. 주요 기능 (3개 이내)
1. **AI 포트폴리오 생성** — 목표 월배당금액 + 월 투자계획금액을 입력하면, Gemini가 배당킹/귀족주 큐레이션 리스트 안에서 각 종목의 실제 배당지급월을 합쳤을 때 12개월이 전부 커버되는 배분안 2개(서로 다른 트레이드오프)를 제안한다.
2. **배당 성장 시뮬레이터** — AI는 배분과 조언만 하고, 연차별 배당금(1·5·10·15·20·25·30년차, 배당성장 vs 배당성장+재투자)은 서버 코드가 결정론적으로 계산해서 표와 차트로 보여준다. 목표 도달 예상 연차도 함께 표시된다.
3. **플랜 저장 & 배당지급 캘린더** — 후보 중 하나를 고르면 그 조합만 Supabase에 저장되고, 상세화면에서 포트폴리오 비중 파이차트, 종목별 상세, 월별 배당지급 캘린더(어느 달에 어느 종목에서 얼마가 들어오는지)를 확인할 수 있다. 플랜 이름 변경/삭제, 입력값을 바꿔 새 플랜으로 재생성하는 것도 가능하다.

## 4. AI를 어디에 어떻게 썼나요
- AI가 하는 일 : 종목별 배분(티커/비중), 후보 컨셉 한 줄, 전략 조언 텍스트 — 딱 이 3가지만. 연차별 배당금 계산이나 목표 도달 시점 판단은 AI에게 맡기지 않고 서버 코드가 확정값으로 처리한다.
- 입력으로 무엇을 주고, 무엇을 받나요 : 목표 월배당금액, 월 투자계획금액, 그리고 Supabase `dividend_stocks`에 저장된 배당주 리스트(티커/수익률/연속증가연수/섹터/배당지급월)를 JSON으로 프롬프트에 넣는다. `responseSchema`로 JSON 응답을 강제해서 후보 2개(배분표+컨셉+조언)를 받는다.
- AI가 이상한 답을 줬을 때 어떻게 처리했나요 : 서버에서 세 가지를 검증한다 — (1) 리스트에 없는 티커(환각) (2) 비중 합계가 100이 아님 (3) 배당이 안 들어오는 달이 있음. 하나라도 걸리면 파싱 실패와 동일하게 처리해서 에러 메시지 + "다시 시도" 버튼을 보여주고 입력값은 그대로 유지한다. 목표 자체가 이 종목 리스트로는 물리적으로 불가능한 경우엔 AI를 호출하기 전에 서버가 먼저 걸러내서(아래 8번 참고), 20초 가까이 걸리는 호출을 아낀다.
- 프롬프트에서 신경 쓴 점 1가지 : "매달 배당이 들어오는 것"이 이 서비스의 핵심 가치라서, 각 종목의 실제 배당지급월(payout_months)을 그대로 프롬프트에 주고 "후보 하나에 포함된 종목들의 지급월을 다 합치면 1~12월이 빠짐없이 커버돼야 한다"는 규칙을 못박았다. 처음엔 지급월을 A/B/C 3개 그룹으로 단순화해서 넣으려 했는데, 그룹으로 뭉치면 그룹 안에서 겹치는 달이 생겨 오히려 부정확해서 원본 지급월 배열을 그대로 쓰는 쪽으로 바꿨다.

## 5. 데이터 설계
- 테이블 이름과 역할 :
  - `dividend_stocks` : 큐레이션한 배당킹/배당귀족 종목의 마스터 데이터. AI 프롬프트의 재료이자 `/stocks` 페이지에 그대로 노출된다.
  - `plans` : 사용자가 후보 2개 중 "선택"한 조합만 저장한다. 선택 안 한 후보는 저장하지 않는다.
- 주요 컬럼 :
  - `dividend_stocks` : ticker, name, sector, dividend_yield, dividend_growth_5y, consecutive_years, payout_months(지급월 배열), business_summary
  - `plans` : anonymous_id / user_id, target_monthly_dividend, monthly_investment, allocations(jsonb, 생성 시점 주가 스냅샷 포함), chart_data(jsonb, 연차별 계산 결과), advice_text, goal_achieved, name
- 이렇게 설계한 이유 : allocations/chart_data를 jsonb로 통째로 저장해두면 나중에 종목 정보나 계산 로직이 바뀌어도 이미 저장된 플랜은 저장 당시 값 그대로 보여줄 수 있다(스냅샷). 로그인 없이도 쓸 수 있어야 해서 브라우저에 저장하는 anonymous_id를 기본 식별자로 두고, 이후 로그인 기능을 붙이면서 user_id 컬럼을 추가해 로그인 사용자는 계정 기준, 비로그인 사용자는 anonymous_id 기준으로 자기 플랜만 조회하게 했다.

## 6. 화면 흐름
1. 로그인/회원가입(이메일+비밀번호, Supabase Auth) 후 홈에서 "배당주 투자가 뭔지 자세히 보기(`/about`)" 또는 "바로 플랜 짜기(`/plan`)" 중 선택한다.
2. `/plan`에서 목표 월배당금액과 월 투자계획금액을 입력하고 "플랜 만들기"를 누르면, 같은 화면에 AI가 만든 후보 2개(배분표 + 30년 시나리오 차트 + 조언 텍스트)가 나타난다.
3. 카드 하나를 "이 플랜 선택하기"로 고르면 그 조합만 Supabase에 저장되고 `/plans/[id]` 상세화면으로 이동한다. 상세화면은 탭으로 "플랜 상세(비중 파이차트+종목별 정보) / 배당 시뮬레이터(연차별 재투자 시나리오) / 배당지급일(월별 캘린더)"을 오가며 볼 수 있고, "입력값 수정하기"로 다시 생성하면 새 플랜으로 따로 저장된다.
4. `/plans`에서 지금까지 만든 플랜들을 카드로 모아보고, 이름을 바꾸거나 삭제할 수 있다.

## 7. 기획서와 달라진 점
- 뺀 것 1 : 희망 기간(년) 범위 입력. 기획 당시엔 "몇 년 안에 목표를 채우고 싶은지"를 min~max로 따로 받으려 했다.
- 뺀 이유 : 실제로 짜보니 월 투자금만 정해지면 도달 시점은 계산으로 나오는 값이라, 사용자가 기간을 직접 입력할 필요가 없었다. 대신 서버가 1~30년 전체를 계산해서 목표가 언제쯤 채워지는지 시나리오로 보여주는 쪽으로 바꿨다.
- 뺀 것 2 : 지급월 A/B/C 3분류. 기획서에서는 이해하기 쉽게 지급월을 세 그룹으로 단순화했었다.
- 뺀 이유 : 그룹으로 뭉치면 그룹 안에서 지급월이 겹쳐서 오히려 "매달 커버" 검증이 부정확해졌다. 종목별 실제 지급월 배열을 그대로 쓰는 게 더 정확해서 그쪽으로 정리했다.
- 시간이 더 있었다면 넣었을 것 : 실시간 시세 API를 계속 갱신해서 "지금 이 가격이면 몇 주를 살 수 있는지"까지 자동 계산해주는 기능. 지금은 플랜 생성 시점의 주가를 한 번 스냅샷으로 저장하는 정도까지만 했다.

## 8. 만들면서 가장 오래 막혔던 것
- 문제 : AI가 짤 법한 배분안으로 이 목표가 물리적으로 도달 가능한지 미리 걸러낼 기준을 잡는 게 어려웠다. 처음엔 "종목 1개에 100% 몰아넣었을 때의 이론적 최댓값"을 상한으로 썼는데, 실제로 AI가 만들 만한 포트폴리오(여러 종목 분산 + 12개월 커버 제약)와 거의 10배 가까이 차이가 나서 판단 기준으로 쓸 수가 없었다.
- 어떻게 해결했나 : `realisticBestAnnualDividend`라는 함수를 새로 만들어서, "고배당 위주로 상위 종목을 고르되 빠진 달만 채워 넣는 방식"과 "배당수익률+성장률 합산 기준으로 고르는 방식" 두 가지로 실제 포트폴리오를 구성해보고 그중 더 나은 값을 현실적 상한으로 삼았다. 여기에 1.5배 정도 여유(AI가 더 잘 짤 수도 있으니)를 두고, 그 상한을 넘는 목표는 Gemini를 호출하기 전에 서버에서 바로 걸러서 "월 얼마 이상 투자하거나 목표를 낮춰달라"고 구체적으로 안내하게 만들었다.
